import { FC } from 'react'

const Home: FC = () => {
	return (
		<div className="flex items-center justify-center min-h-screen">
			<p>
				Get started by editing
				<code className="px-1 py-0.5 rounded font-mono font-semibold">src/app/page.tsx</code>
			</p>
		</div>
	)
}

export default Home
